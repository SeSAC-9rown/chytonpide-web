<div class="min-h-screen bg-gray-50 flex items-center justify-center px-4 py-12">
  <div class="max-w-md w-full">
    <!-- Header -->
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-2">프로필 설정</h1>
      <p class="text-gray-600">반갑습니다! 닉네임을 입력하고 가입을 완료하세요.</p>
    </div>

    <!-- Card Container -->
    <div class="bg-white rounded-lg shadow-md p-8">
      <!-- Error Messages -->
      <% if @user.errors.any? %>
        <div class="mb-6 bg-red-50 border border-red-200 rounded-lg p-4">
          <h3 class="text-red-800 font-semibold mb-2">
            <%= pluralize(@user.errors.count, "error") %> 발생:
          </h3>
          <ul class="list-disc list-inside text-red-700 text-sm space-y-1">
            <% @user.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <!-- Profile Image -->
      <% if @user.social_avatar_url.present? %>
        <div class="flex justify-center mb-6">
          <%= image_tag @user.social_avatar_url, 
              alt: "프로필 이미지", 
              class: "w-20 h-20 rounded-full object-cover border-4 border-gray-100 shadow-sm" %>
        </div>
      <% end %>

      <!-- Email Display -->
      <div class="mb-6 pb-6 border-b border-gray-200">
        <p class="text-sm text-gray-500 mb-1">이메일</p>
        <p class="text-gray-900 font-medium"><%= @user.email %></p>
      </div>

      <!-- Form -->
      <%= form_with model: @user, url: users_path, method: :post, class: "space-y-6" do |f| %>
        <div>
          <%= f.hidden_field :avatar_url, value: @user.social_avatar_url %>
          <%= f.label :name, "닉네임", class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= f.text_field :name, 
              placeholder: "식집사님을 어떻게 불러드릴까요?", 
              class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent outline-none transition" %>
        </div>

        <div>
          <%= f.submit "가입 완료", 
              class: "w-full bg-green-600 hover:bg-green-700 text-white font-semibold py-3 px-4 rounded-lg transition duration-200 ease-in-out transform hover:scale-[1.02] active:scale-[0.98] shadow-sm" %>
        </div>
      <% end %>
    </div>

    <!-- Footer Text (Optional) -->
    <p class="text-center text-sm text-gray-500 mt-6">
      🌱 식물과 함께하는 즐거운 여정을 시작하세요!
    </p>
  </div>
</div>
