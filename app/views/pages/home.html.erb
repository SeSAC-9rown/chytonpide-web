<body class="bg-gray-100">
  <div class="md:flex min-h-screen">
    <nav class="fixed bottom-0 z-10 w-full bg-white border-t 
               md:border-t-0 md:border-r md:left-0 md:top-0 
               md:h-screen md:w-64 md:flex-shrink-0">

      <div class="hidden md:block p-6 border-b">
        <h1 class="text-2xl font-bold text-gray-800">치톤피드</h1>
      </div>

      <ul class="flex justify-around md:flex-col md:p-4 md:space-y-2">
        <li class="flex-1 md:flex-none">
          <%= link_to root_path,
              class: "flex flex-col items-center justify-center p-3 text-gray-500 hover:text-gray-700 hover:bg-gray-50 md:flex-row md:justify-start md:space-x-3 md:rounded-lg" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
            </svg>
            <span class="text-xs font-medium md:text-base">홈</span>
          <% end %>
        </li>

        <li class="flex-1 md:flex-none">
          <%= link_to log_path,
              class: "flex flex-col items-center justify-center p-3 text-blue-600 bg-blue-50 md:flex-row md:justify-start md:space-x-3 md:rounded-lg" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01" />
            </svg>
            <span class="text-xs font-medium md:text-base">성장 일지</span>
          <% end %>
        </li>

        <li class="flex-1 md:flex-none">
          <%= link_to mypage_path,
              class: "flex flex-col items-center justify-center p-3 text-gray-500 hover:text-gray-700 hover:bg-gray-50 md:flex-row md:justify-start md:space-x-3 md:rounded-lg" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
            <span class="text-xs font-medium md:text-base">마이페이지</span>
          <% end %>
        </li>
      </ul>
    </nav>

    <main class="flex-1 p-6 pb-24 md:pb-6 md:ml-64">
      <% if current_user.device.present? %>
        <% led_mode = current_user.device.led_mode || "AUTO" %>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
          <div class="lg:col-span-2 space-y-6">
            <!-- 메인 카드 -->
            <div class="bg-white p-6 rounded-lg shadow-md h-96 flex flex-col items-center justify-center">
              <img src="https://raw.githubusercontent.com/SeSAC-9rown/9rown-project/refs/heads/main/%EC%9D%B4%EB%AF%B8%EC%A7%80%20(2).svg"
                   alt="영양제 주는 방법 이미지"
                   class="rounded-lg h-full">
              <h3 class="text-2xl font-bold text-gray-800">치피</h3>
              <span class="text-lg text-green-600">기분이 좋아요!</span>
              <p class="text-sm text-gray-600">마지막 업데이트: </p>
              <p class="text-sm text-gray-600" id="sensor-timestamp">...</p>
            </div>

            <!-- 조명 컨트롤 -->
            <div class="bg-white p-6 rounded-lg shadow-md">
              <h3 class="text-xl font-semibold text-gray-800 mb-4">조명 컨트롤</h3>
              <div class="grid grid-cols-3 gap-3">
                <!-- 버튼에 span id를 심어서 현재 상태를 그대로 보여주게 함 -->
                <button id="btn-on"
                        class="py-3 px-4 rounded-lg font-semibold transition
                               <%= led_mode == 'ON' ? 'bg-green-600 text-white shadow' : 'bg-gray-100 hover:bg-gray-200 text-gray-700' %>">
                  ON
                </button>

                <button id="btn-off"
                        class="py-3 px-4 rounded-lg font-semibold transition
                               <%= led_mode == 'OFF' ? 'bg-gray-600 text-white shadow' : 'bg-gray-100 hover:bg-gray-200 text-gray-700' %>">
                  OFF
                </button>

                <button id="btn-auto"
                        class="py-3 px-4 rounded-lg font-semibold transition
                               <%= led_mode == 'AUTO' ? 'bg-blue-600 text-white shadow' : 'bg-gray-100 hover:bg-gray-200 text-gray-700' %>">
                  AUTO
                </button>
              </div>

              <!-- 디버깅/상태 확인용 텍스트 (원하면 숨겨도 됨) -->
              <p class="text-sm text-gray-600 mt-4">
                현재 모드:
                <span id="led-mode"><%= current_user.device.led_mode %></span>,
                LED:
                <span id="is-led-on"><%= current_user.device.is_led_on %></span>,
                LCD:
                <span id="lcd-face"><%= current_user.device.lcd_face %></span>
              </p>

              <p class="text-sm text-gray-600 mt-2">현재 조도: </p>
              <p class="text-sm text-gray-600" id="sensor-illuminance">...</p>
            </div>
          </div>

          <!-- 우측 카드들 (원래 그대로) -->
          <div class="lg:col-span-1 grid grid-cols-2 lg:grid-cols-1 gap-6">
            <div class="bg-white p-6 rounded-lg shadow-md">
              <div class="flex justify-between items-center mb-1">
                <h4 class="text-lg font-semibold text-gray-700">온도</h4>
                <p class="text-2xl md:text-3xl font-bold text-gray-900" id="sensor-temperature">...</p>
                <p class="text-2xl md:text-3xl font-bold text-gray-900">°C</p>
              </div>
              <p class="text-sm text-green-600">적당해요</p>
            </div>

            <div class="bg-white p-6 rounded-lg shadow-md">
              <div class="flex justify-between items-center mb-1">
                <h4 class="text-lg font-semibold text-gray-700">습도</h4>
                <p class="text-2xl md:text-3xl font-bold text-gray-900" id="sensor-humidity">...</p>
                <p class="text-2xl md:text-3xl font-bold text-gray-900">%</p>
              </div>
              <p class="text-sm text-orange-600">건조해요</p>
            </div>

            <div class="bg-white p-6 rounded-lg shadow-md">
              <div class="flex justify-between items-center">
                <h4 class="text-lg font-semibold text-gray-700">물 주기</h4>
                <span class="text-2xl font-bold text-blue-600">D-3</span>
              </div>
            </div>

            <div class="bg-white p-6 rounded-lg shadow-md">
              <div class="flex justify-between items-center">
                <h4 class="text-lg font-semibold text-gray-700">영양제</h4>
                <span class="text-2xl font-bold text-gray-800">D-10</span>
              </div>
            </div>
          </div>
        </div>

        <script>
          (function() {
            // 이 함수 스코프 안에서만 유효
            const userId  = <%= current_user.id %>;
            const csrfTok = document.querySelector('meta[name="csrf-token"]').content;
        
            async function updateDevice(ledMode, isLedOn, lcdFace) {
              const params = new URLSearchParams();
              params.append('led_mode', ledMode);
              params.append('is_led_on', isLedOn);
              params.append('lcd_face', lcdFace);
        
              const res  = await fetch(`/users/${userId}/device`, {
                method: 'PATCH',
                headers: {
                  'Content-Type': 'application/x-www-form-urlencoded;charset=UTF-8',
                  'X-CSRF-Token': csrfTok
                },
                body: params.toString()
              });
        
              const data = await res.json();
              console.log('PATCH result', res.status, data);
        
              if (res.ok && data.status === 'success') {
                document.getElementById('led-mode').textContent  = ledMode;
                document.getElementById('is-led-on').textContent = isLedOn;
                document.getElementById('lcd-face').textContent  = lcdFace;
                setLedButtonsUI(ledMode);
              } else {
                alert('상태 업데이트에 실패했습니다.');
              }
            }
        
            function setLedButtonsUI(mode) {
              const onBtn   = document.getElementById('btn-on');
              const offBtn  = document.getElementById('btn-off');
              const autoBtn = document.getElementById('btn-auto');
        
              const reset = (btn) => {
                btn.classList.remove('bg-green-600', 'bg-gray-600', 'bg-blue-600', 'text-white', 'shadow');
                btn.classList.add('bg-gray-100', 'text-gray-700');
              };
        
              [onBtn, offBtn, autoBtn].forEach(reset);
        
              if (mode === 'ON') {
                onBtn.classList.remove('bg-gray-100', 'text-gray-700');
                onBtn.classList.add('bg-green-600', 'text-white', 'shadow');
              } else if (mode === 'OFF') {
                offBtn.classList.remove('bg-gray-100', 'text-gray-700');
                offBtn.classList.add('bg-gray-600', 'text-white', 'shadow');
              } else if (mode === 'AUTO') {
                autoBtn.classList.remove('bg-gray-100', 'text-gray-700');
                autoBtn.classList.add('bg-blue-600', 'text-white', 'shadow');
              }
            }
        
            document.addEventListener('turbo:load', () => {
              // 초기 상태 반영
              setLedButtonsUI('<%= led_mode %>');
        
              document.getElementById('btn-on').addEventListener('click', () => {
                updateDevice('ON', 'true', 'HAPPY');
              });
        
              document.getElementById('btn-off').addEventListener('click', () => {
                updateDevice('OFF', 'false', 'SAD');
              });
        
              document.getElementById('btn-auto').addEventListener('click', () => {
                updateDevice('AUTO', 'true', 'HAPPY');
              });
            });
          })();
        </script>


      <% else %>
        <div class="flex flex-col items-center justify-center h-96">
          <div class="text-center">
            <h2 class="text-3xl font-bold text-gray-800 mb-4">등록된 디바이스가 없습니다</h2>
            <p class="text-lg text-gray-600 mb-8">첫 번째 화분 친구를 등록해보세요!</p>

            <%= link_to "디바이스 등록",
                new_user_device_path(current_user),
                method: :get,
                class: "py-3 px-6 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition" %>
          </div>
        </div>
      <% end %>
    </main>
  </div>
</body>